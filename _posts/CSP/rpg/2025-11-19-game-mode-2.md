---
layout: post
title: Game Mode Selection
description: Let the user choose the type of RPG they want to create, affecting the examples and style shown later.
permalink: /rpg/mode
comments: True
---

<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<div class="min-h-screen bg-black py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-[1600px] mx-auto w-full">
    <!-- Header Section -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold mb-4 text-white">Choose Your RPG Adventure</h1>
      <p class="text-xl max-w-3xl mx-auto text-gray-200">
        Select the type of RPG experience you want to create. This will shape the examples and style recommendations throughout the creation process.
      </p>
    </div>

    <!-- Main Content: Side by Side -->
    <div class="grid grid-cols-1 lg:grid-cols-5 gap-8 mb-8">
      
      <!-- Left Side: Game Mode Cards (3 columns) -->
      <div class="lg:col-span-3">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <!-- Chill / Cozy RPG Card -->
          <div class="game-mode-card group" data-mode="chill">
            <div class="game-mode-card-inner">
              <div class="game-mode-image-container">
                <div class="game-mode-image rounded-t-2xl h-48 flex items-center justify-center bg-gradient-to-br from-green-700 to-blue-700">
                  <div class="text-center">
                    <div class="text-6xl mb-2">🌿</div>
                    <div class="text-sm font-medium text-gray-200">Calm & Exploratory</div>
                  </div>
                </div>
              </div>
              <div class="game-mode-content">
                <h3 class="game-mode-title">Chill / Cozy RPG</h3>
                <p class="game-mode-description text-gray-300">
                  Relaxed pace, exploration-focused gameplay with emphasis on storytelling, character development, and atmospheric world-building.
                </p>
                <ul class="game-mode-features text-gray-200">
                  <li>✓ Peaceful exploration</li>
                  <li>✓ Rich narrative depth</li>
                  <li>✓ Character relationships</li>
                  <li>✓ Low-stress gameplay</li>
                </ul>
              </div>
              <div class="game-mode-selection-indicator">
                <div class="selection-dot"></div>
              </div>
            </div>
          </div>

          <!-- Action RPG Card -->
          <div class="game-mode-card group" data-mode="action">
            <div class="game-mode-card-inner">
              <div class="game-mode-image-container">
                <div class="game-mode-image rounded-t-2xl h-48 flex items-center justify-center bg-gradient-to-br from-red-700 to-orange-700">
                  <div class="text-center">
                    <div class="text-6xl mb-2">⚔️</div>
                    <div class="text-sm font-medium text-gray-200">Fast & Exciting</div>
                  </div>
                </div>
              </div>
              <div class="game-mode-content">
                <h3 class="game-mode-title">Action RPG</h3>
                <p class="game-mode-description text-gray-300">
                  Fast-paced, combat-driven experience with intense battles, quick decision-making, and adrenaline-pumping encounters.
                </p>
                <ul class="game-mode-features text-gray-200">
                  <li>✓ Dynamic combat system</li>
                  <li>✓ Quick reflexes required</li>
                  <li>✓ Epic boss battles</li>
                  <li>✓ High-intensity moments</li>
                </ul>
              </div>
              <div class="game-mode-selection-indicator">
                <div class="selection-dot"></div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Right Side: Statistics (2 columns) -->
      <div class="lg:col-span-2">
        <div class="rounded-2xl shadow-xl p-6 bg-[#111] h-full">
          <div class="text-center mb-6">
            <h2 class="text-2xl font-bold mb-2 text-white">Community Preferences</h2>
            <p class="text-gray-400 text-sm">See what other creators are choosing</p>
          </div>

          <!-- Stats Cards -->
          <div class="space-y-4 mb-6">
            <!-- Chill -->
            <div class="flex items-center justify-between p-4 rounded-lg bg-green-900/40">
              <div class="flex items-center">
                <div class="w-10 h-10 rounded-full bg-green-800 flex items-center justify-center mr-3">
                  <span class="text-xl">🌿</span>
                </div>
                <div>
                  <h4 class="font-bold text-white text-sm">Chill / Cozy RPG</h4>
                  <p class="text-xs text-gray-300">Calm & Exploratory</p>
                </div>
              </div>
              <div class="text-right">
                <div class="text-2xl font-bold text-white" id="chill-count">0</div>
                <div class="text-xs text-gray-300" id="chill-percentage">0%</div>
              </div>
            </div>

            <!-- Action -->
            <div class="flex items-center justify-between p-4 rounded-lg bg-red-900/40">
              <div class="flex items-center">
                <div class="w-10 h-10 rounded-full bg-red-800 flex items-center justify-center mr-3">
                  <span class="text-xl">⚔️</span>
                </div>
                <div>
                  <h4 class="font-bold text-white text-sm">Action RPG</h4>
                  <p class="text-xs text-gray-300">Fast & Exciting</p>
                </div>
              </div>
              <div class="text-right">
                <div class="text-2xl font-bold text-white" id="action-count">0</div>
                <div class="text-xs text-gray-300" id="action-percentage">0%</div>
              </div>
            </div>

            <!-- Total -->
            <div class="flex items-center justify-between p-4 rounded-lg bg-blue-900/40">
              <div>
                <h4 class="font-bold text-white text-sm">Total Votes</h4>
                <p class="text-xs text-gray-300">All community selections</p>
              </div>
              <div class="text-right">
                <div class="text-2xl font-bold text-white" id="total-count">0</div>
                <div class="text-xs text-gray-300">users</div>
              </div>
            </div>
          </div>

          <!-- Chart -->
          <div class="rounded-xl p-4 bg-[#222]">
            <h3 class="text-lg font-bold mb-4 text-white">Visual Distribution</h3>
            <div class="h-64">
              <canvas id="stats-chart"></canvas>
            </div>
          </div>

          <div class="mt-4 text-center">
            <button id="reset-stats" class="text-xs underline text-gray-400 hover:text-white">
              Reset Demo Data
            </button>
          </div>
        </div>
      </div>

    </div>

    <!-- Continue Button - ONLY ONE -->
    <div class="text-center mt-12 mb-8">
      <button id="continue-btn" class="continue-button" disabled>
        Continue to Next Step
      </button>
      <p id="selection-hint" class="mt-4 text-gray-300">
        Please select a game mode to continue
      </p>
    </div>

    <!-- Info Note -->
    <div class="text-center text-xs text-gray-500 mb-4">
      <p>Statistics are stored locally in your browser for demonstration purposes.</p>
    </div>

  </div>
</div>

<style>
  /* 突破 Jekyll 主题的宽度限制 */
  .page-content .wrapper {
    max-width: 100% !important;
    width: 100% !important;
    padding: 0 !important;
    margin: 0 !important;
  }
  
  .page-content {
    width: 100% !important;
    max-width: 100% !important;
    padding: 0 !important;
    margin: 0 !important;
  }
  
  body, h1, h2, h3, h4, p, span, div, button { color: #fff !important; }
  .game-mode-card { border-radius: 1rem; border: 1px solid #333; background-color: #111 !important; transition: all 0.3s ease; cursor: pointer; position: relative; }
  .game-mode-card:hover { transform: translateY(-4px); box-shadow: 0 0 20px rgba(255,255,255,0.1); }
  .game-mode-card.selected { border: 2px solid #8b5cf6; animation: pulse 0.5s ease; box-shadow: 0 0 20px rgba(139,92,246,0.4); }
  .game-mode-content { padding: 1.5rem; }
  .game-mode-title { font-size: 1.5rem; font-weight: bold; margin-bottom: 0.75rem; }
  .game-mode-description { margin-bottom: 0.75rem; line-height: 1.6; }
  .game-mode-features { list-style: none; padding: 0; margin-top: 0.75rem; }
  .game-mode-features li { margin-bottom: 0.5rem; }
  .game-mode-selection-indicator { position: absolute; top: 0.75rem; right: 0.75rem; width: 1.75rem; height: 1.75rem; border: 2px solid #fff; border-radius: 50%; display: flex; justify-content: center; align-items: center; }
  .game-mode-card.selected .game-mode-selection-indicator { background-color: #8b5cf6; border-color: #8b5cf6; }
  .selection-dot { width: 0.65rem; height: 0.65rem; background-color: #fff; border-radius: 50%; opacity: 0; }
  .game-mode-card.selected .selection-dot { opacity: 1; }
  .continue-button { background-color: #8b5cf6; color: #fff !important; padding: 0.75rem 2rem; border-radius: 0.75rem; font-weight: 600; font-size: 1.125rem; opacity: 0.5; transition: all 0.3s ease; border: none; cursor: pointer; }
  .continue-button:not(:disabled) { opacity: 1; }
  .continue-button:not(:disabled):hover { transform: translateY(-2px); box-shadow: 0 0 20px rgba(139,92,246,0.5); }
  
  @media (max-width: 1023px) {
    .lg\\:col-span-3, .lg\\:col-span-2 { grid-column: span 1; }
  }
</style>

<script>
  class StatisticsManager {
    constructor() {
      this.storageKey = 'rpgModeStatistics_fallback';
      this.stats = { chill: 0, action: 0, total: 0, history: [] };
      this.chart = null;
      this.API_BASE = "http://localhost:8587/api";
      this.ENDPOINTS = {
        getStats: `${this.API_BASE}/rpg_stats`,
        record: `${this.API_BASE}/rpg_stats/record`,
        reset: `${this.API_BASE}/rpg_stats/reset`,
        health: `${this.API_BASE}/rpg_stats/health`,
      };
      this.statusEl = null;
    }

    attachStatusEl(el) { this.statusEl = el; }

    getLocalUserSession() {
      try { return JSON.parse(localStorage.getItem("userSession") || "{}"); }
      catch (e) { return {}; }
    }

    setStatus(message, type = "info") {
      if (!this.statusEl) return;
      const base = "mt-4 text-sm";
      const styles = {
        info: `${base} text-gray-300`,
        ok: `${base} text-green-300`,
        warn: `${base} text-yellow-300`,
        err: `${base} text-red-300`
      };
      this.statusEl.className = styles[type] || styles.info;
      this.statusEl.textContent = message;
    }

    async getUserGithubId() {
      const s = this.getLocalUserSession();
      if (s?.githubId) return String(s.githubId);
      let userId = sessionStorage.getItem('rpgUserId');
      if (userId) return userId;
      userId = 'user_' + Math.random().toString(36).substr(2, 9);
      sessionStorage.setItem('rpgUserId', userId);
      return userId;
    }

    async fetchJSON(url, options = {}) {
      const res = await fetch(url, {
        headers: { 'Content-Type': 'application/json' },
        ...options
      });
      let body = null;
      try { body = await res.json(); } catch (e) {}
      if (!res.ok) {
        const msg = body?.error || body?.message || `HTTP ${res.status}`;
        throw new Error(msg);
      }
      return body;
    }

    async loadFromBackend() {
      const data = await this.fetchJSON(this.ENDPOINTS.getStats, { method: 'GET' });
      this.stats = this.normalizeStats(data);
      this.updateDisplay();
      this.setStatus("✅ Loaded live stats from backend", "ok");
    }

    async recordSelectionBackendamode) {
      const userGithubId = await this.getUserGithubId();
      const data = await this.fetchJSON(this.ENDPOINTS.record, {
        method: 'POST',
        body: JSON.stringify({ mode, userGithubId })
      });
      this.stats = this.normalizeStats(data);
      this.updateDisplay();
      this.setStatus(`✅ Recorded '${mode}' to backend as ${userGithubId}`, "ok");
    }

    async resetBackend() {
      const data = await this.fetchJSON(this.ENDPOINTS.reset, { method: 'POST' });
      this.stats = this.normalizeStats(data);
      this.updateDisplay();
      this.setStatus("✅ Reset stats on backend", "ok");
    }

    normalizeStats(data) {
      const chill = Number(data?.chill ?? 0);
      const action = Number(data?.action ?? 0);
      const total = Number(data?.total ?? (chill + action));
      const history = Array.isArray(data?.history) ? data.history : [];
      return { chill, action, total, history };
    }

    loadFallback() {
      const defaultStats = { chill: 0, action: 0, total: 0, history: [] };
      try {
        const stored = localStorage.getItem(this.storageKey);
        return stored ? JSON.parse(stored) : defaultStats;
      } catch (e) { return defaultStats; }
    }

    saveFallback() {
      try { localStorage.setItem(this.storageKey, JSON.stringify(this.stats)); }
      catch (e) {}
    }

    async recordSelectionFallback(mode) {
      const userGithubId = await this.getUserGithubId();
      const timestamp = new Date().toISOString();
      this.stats.history.push({ userGithubId, mode, timestamp });
      if (mode === 'chill' || mode === 'action') {
        this.stats[mode]++;
        this.stats.total++;
      }
      this.saveFallback();
      this.updateDisplay();
      this.setStatus("⚠️ Backend unreachable — using local demo stats", "warn");
    }

    resetFallback() {
      this.stats = { chill: 0, action: 0, total: 0, history: [] };
      this.saveFallback();
      this.updateDisplay();
      this.setStatus("⚠️ Backend unreachable — reset local demo stats", "warn");
    }

    getPercentage(mode) {
      if (this.stats.total === 0) return 0;
      return Math.round((this.stats[mode] / this.stats.total) * 100);
    }

    updateDisplay() {
      document.getElementById('chill-count').textContent = this.stats.chill;
      document.getElementById('action-count').textContent = this.stats.action;
      document.getElementById('total-count').textContent = this.stats.total;
      document.getElementById('chill-percentage').textContent = `${this.getPercentage('chill')}%`;
      document.getElementById('action-percentage').textContent = `${this.getPercentage('action')}%`;
      this.updateChart();
    }

    updateChart() {
      if (!this.chart) return;
      this.chart.data.datasets[0].data = [this.stats.chill, this.stats.action];
      this.chart.update();
    }

    initChart() {
      const ctx = document.getElementById('stats-chart').getContext('2d');
      Chart.defaults.color = '#fff';
      Chart.defaults.font.family = "'Inter', sans-serif";
      this.chart = new Chart(ctx, {
        type: 'doughnut',
        data: {
          labels: ['Chill / Cozy', 'Action'],
          datasets: [{
            data: [this.stats.chill, this.stats.action],
            backgroundColor: ['rgba(34,197,94,0.8)', 'rgba(239,68,68,0.8)'],
            borderColor: ['rgb(34,197,94)', 'rgb(239,68,68)'],
            borderWidth: 2
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: {
            legend: {
              position: 'bottom',
              labels: { color: '#fff', padding: 15, font: { size: 12 } }
            },
            tooltip: {
              backgroundColor: 'rgba(0,0,0,0.9)',
              bodyColor: '#fff',
              titleColor: '#fff'
            }
          }
        }
      });
    }
  }

  document.addEventListener('DOMContentLoaded', async function () {
    const gameModeCards = document.querySelectorAll('.game-mode-card');
    const continueBtn = document.getElementById('continue-btn');
    const selectionHint = document.getElementById('selection-hint');
    const resetStatsBtn = document.getElementById('reset-stats');
    let selectedMode = null;

    const statsManager = new StatisticsManager();
    statsManager.initChart();
    statsManager.updateDisplay();

    const statusEl = document.createElement("p");
    statusEl.className = "mt-2 text-sm text-gray-400";
    selectionHint.insertAdjacentElement("afterend", statusEl);
    statsManager.attachStatusEl(statusEl);

    try {
      await statsManager.loadFromBackend();
    } catch (e) {
      statsManager.stats = statsManager.loadFallback();
      statsManager.updateDisplay();
      statsManager.setStatus(`⚠️ Backend not reachable (${e.message}). Using local demo stats.`, "warn");
    }

    if (!sessionStorage.getItem('rpgCreatorPreferences')) {
      sessionStorage.setItem('rpgCreatorPreferences', JSON.stringify({}));
    }

    gameModeCards.forEach(card => {
      card.addEventListener('click', async function () {
        const mode = this.dataset.mode;
        gameModeCards.forEach(c => c.classList.remove('selected'));
        this.classList.add('selected');
        selectedMode = mode;
        continueBtn.disabled = false;
        selectionHint.textContent = `Selected: ${mode}`;
        selectionHint.className = 'font-medium mt-4 text-white';
        const preferences = JSON.parse(sessionStorage.getItem('rpgCreatorPreferences'));
        preferences.gameMode = mode;
        sessionStorage.setItem('rpgCreatorPreferences', JSON.stringify(preferences));
        statsManager.setStatus("ℹ️ Mode selected — press Continue to save your vote.", "info");
      });
    });

    let hasRecordedMode = false;

    continueBtn.addEventListener('click', async function () {
      if (!selectedMode) return;
      if (hasRecordedMode) return;
      hasRecordedMode = true;
      continueBtn.disabled = true;
      try {
        await statsManager.recordSelectionBackend(selectedMode);
      } catch (e) {
        statsManager.stats = statsManager.loadFallback();
        await statsManager.recordSelectionFallback(selectedMode);
      }
      window.location.href = "/rpg/story";
    });

    resetStatsBtn.addEventListener('click', async function () {
      if (!confirm("Are you sure?")) return;
      try {
        await statsManager.resetBackend();
      } catch (e) {
        statsManager.resetFallback();
      }
    });
  });
</script>